defaults: &defaults

development:
  <<: *defaults
  host: localhost
  database: grbr_development

test:
  <<: *defaults
  host: localhost
  database: grbr_test

production:
  #uri: <%= ENV['MONGOHQ_URL'] %>
  <<: *defaults
   	host: localhost
   	database: grbr_production


